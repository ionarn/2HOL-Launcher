extends Node

signal _ready_populate_emotion_lineedit
signal emotion_ready(node)
signal ready_load_checkbutton(checkbutton, reset_button, ini_path, default_setting, invert_bool)
signal ready_load_spinbox(spinbox_node, ini_path, reset_button, default_setting)
